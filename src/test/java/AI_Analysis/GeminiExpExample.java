package AI_Analysis;

import com.google.genai.Client;
import com.google.genai.types.Content;
import com.google.genai.types.GenerateContentResponse;
//import com.google.genai.types.ResponseModality;

public class GeminiExpExample {
    public static void main(String[] args) {
//        String apiKey = "YOUR_API_KEY";
//
//        // Create the client
//        Client client = Client.builder()
//                .apiKey(apiKey)
//                .build();
//
//        // Create the model
//        var model = client.models().get("gemini-2.0-flash-exp");
//
//        // Build the input content
//        Content input = Content.of(
//                "Generate a story about a cute baby turtle in a 3d digital art style. For each scene, generate an image."
//        );
//
//        // Call the API
//        GenerateContentResponse response = model.generateContent(input);
//
//        // Print the generated text
//        System.out.println("Text: " + response.text());
//
//        // Print the generated image URIs
//        response.images().forEach(img -> {
//            System.out.println("Image URI: " + img.uri());
//        });
    }
}
